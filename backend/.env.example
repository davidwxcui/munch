MONGODB_URI=mongodb://localhost:27017/restaurant-match
GOOGLE_PLACES_API_KEY=your_google_places_api_key_here
PORT=5000
NODE_ENV=development
FRONTEND_URL=http://localhost:5173

# Rate Limiting Configuration (optional - defaults will be used if not set)
# Per-IP rate limit: 100 requests per 15 minutes by default
RATE_LIMIT_WINDOW_MS=900000
RATE_LIMIT_MAX_REQUESTS=100

# Strict rate limit: 50 requests per hour by default
STRICT_RATE_LIMIT_MAX_REQUESTS=50

# Global rate limit: 10,000 requests per day by default
GLOBAL_RATE_LIMIT_MAX_REQUESTS=10000
